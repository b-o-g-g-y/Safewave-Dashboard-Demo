import React from "react";
import ReactDOM from "react-dom/client";
import SafewaveDashboard from "../dashboard.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SafewaveDashboard />
  </React.StrictMode>
);
